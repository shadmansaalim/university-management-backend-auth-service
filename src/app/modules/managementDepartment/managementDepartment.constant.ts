// Searching and Filtering  Fields
const filterableFields = ['searchTerm', 'title'];

// Searchable fields to GET management departments
const searchableFields = ['title'];

export const ManagementDepartmentConstants = {
  filterableFields,
  searchableFields,
};
